<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hackathon">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileUpload">

        </activity>
        <activity android:name=".ProfileView" />
        <activity android:name=".BedFilter">

        </activity>
        <activity android:name=".NewUser" />
        <activity android:name=".Register" >

        </activity>
        <activity android:name=".Login" />
        <activity android:name=".ViewDetaail" />
        <activity android:name=".ProfilePicEdit" />
        <activity android:name=".Notification_Filter" />
        <activity android:name=".Notification_view" />
        <activity android:name=".Dash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>